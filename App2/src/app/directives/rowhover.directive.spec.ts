import { RowhoverDirective } from './rowhover.directive';

describe('RowhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new RowhoverDirective();
    expect(directive).toBeTruthy();
  });
});
